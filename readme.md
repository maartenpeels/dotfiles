# Dotfiles
This repo contains everything needed to bootstrap a new system.

## Packages
- Vim

## Usage
To bootstrap a new system, run:
```
make install    # Installs the packages
make link       # Links the dotfiles to ~/ or ~/.config/
```

Or, both at the same time:
```
make            # Installs and links
```